PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/tictactoe_realtime
JWT_SECRET=replace_with_a_long_random_secret
CLIENT_URL=http://localhost:5173
# Set USE_MEMORY_MONGO=true to use an in-memory MongoDB for local dev (no install needed)
# Requires mongodb-memory-server in devDependencies
USE_MEMORY_MONGO=false
